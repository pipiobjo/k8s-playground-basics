apiVersion: v1
data:
  config.yaml: |-
    service:
      rest: 8080
      health: 8081
      logLevel: debug
kind: ConfigMap
metadata:
  name: greeting-props-dtd26868b8
  namespace: prod
---
apiVersion: v1
kind: Service
metadata:
  name: greeting
  namespace: prod
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: greeting
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: greeting
  name: greeting
  namespace: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: greeting
  template:
    metadata:
      labels:
        app: greeting
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.azure.com/agentpool
                operator: NotIn
                values:
                - system
      containers:
      - image: localhost:5003/development/greeting:tilt-build-1666675944
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /live
            port: 8081
          initialDelaySeconds: 2
          periodSeconds: 15
        name: greeting
        ports:
        - containerPort: 8080
        - containerPort: 8081
        readinessProbe:
          httpGet:
            path: /start
            port: 8081
          initialDelaySeconds: 2
          periodSeconds: 15
        resources:
          limits:
            cpu: 500m
            memory: 200Mi
          requests:
            cpu: 20m
            memory: 20Mi
        volumeMounts:
        - mountPath: /config
          name: config-volume
      imagePullSecrets:
      - name: ofcsecret
      volumes:
      - configMap:
          items:
          - key: config.yaml
            path: config.yaml
          name: greeting-props-dtd26868b8
        name: config-volume
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: greeting
  namespace: prod
spec:
  maxReplicas: 5
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: greeting
  targetCPUUtilizationPercentage: 70
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  name: greeting
  namespace: prod
spec:
  ingressClassName: nginx
  rules:
  - host: prod.internal
    http:
      paths:
      - backend:
          service:
            name: greeting
            port:
              number: 80
        path: /greeting(/|$)(.*)
        pathType: Prefix
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  name: greeting-path
  namespace: prod
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - backend:
          service:
            name: greeting
            port:
              number: 80
        path: /prod/greeting(/|$)(.*)
        pathType: Prefix
